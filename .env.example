# Copy to .env. Never commit real values.

# Optional Telegram alerts (leave blank to skip)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Override default SQLite path if needed
# EVENT_RADAR_DB=data/events.db

# Polite User-Agent identity
# EVENT_RADAR_USER_AGENT=BostonEventRadar/0.1 (personal research)
